# Copy to .env and replace these local-development placeholders.
MYSQL_ROOT_PASSWORD=change-me-local-root-password
# Match DB_PASSWORD in src/TaskList/.env.
MYSQL_PASSWORD=change-me-local-db-password
